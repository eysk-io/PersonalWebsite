import styled from 'styled-components';

const PortfolioStyles = styled.div`
    .LeftButton {
        position: relative;
    }
    .RightButton {
        position: relative;
    }
`

export default PortfolioStyles;