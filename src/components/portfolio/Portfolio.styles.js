import styled from 'styled-components';

const PortfolioStyles = styled.div`
    .portfolio {
        margin: auto;
        text-align: center;
        padding-top: 150px;
        max-width: 100%;
    }
    .LeftButton {
        position: relative;
    }
    .RightButton {
        position: relative;
    }
`

export default PortfolioStyles;