import styled from 'styled-components';

const PortfolioStyles = styled.div`
    .portfolio {
        text-align: center;
    }
    .LeftButton {
        position: relative;
    }
    .RightButton {
        position: relative;
    }
`

export default PortfolioStyles;