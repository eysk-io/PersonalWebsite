import styled from 'styled-components';

const PortfolioStyles = styled.div`
    .portfolio {
        margin: auto;
        text-align: center;
        padding-top: 150px;
        max-width: 750px;
    }
    .LeftButton {
        position: relative;
    }
    .RightButton {
        position: relative;
    }
`

export default PortfolioStyles;