import styled from 'styled-components';

const LeftButtonStyles = styled.div`
    a {
        color: #000;
    }
    .left-button {
        position: absolute;
        top: 50%;
        left: 30%;
    }
`;

export default LeftButtonStyles